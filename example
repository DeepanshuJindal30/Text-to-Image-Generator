await api.put(`/Trustee/${editId}`, {
  Name: form.name,
  Email: form.email,
  Phone: form.phone,
  Plan_Number: form.planNumber,
});


await api.post("/Trustee", {
  Name: form.name,
  Email: form.email,
  Phone: form.phone,
  Plan_Number: form.planNumber,
});