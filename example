(contactdelete && contactdelete.contacts || []).forEach(function (localContact) {
  const type = util
    .replaceNullorEmpty(localContact && localContact.contactType ? localContact.contactType : "")
    .toUpperCase();

  if (type === Constants.CONTACT_TYPES_MANDATORY_RULE.ADMIN1) {
    isAdmin1ToDelete = true;
    admin1ContactToDelete = localContact && localContact.contactId ? localContact.contactId : "";
  }
});